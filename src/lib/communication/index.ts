// Team Hub — data access, hooks, and realtime subscriptions.
//
// Phase 1 (current): types only. Build sequence per spec §9:
//   Phase 2 — server data access + Server Actions (channel/thread/message CRUD)
//   Phase 3 — mention queries + filter views
//   Phase 4 — realtime subscriptions (messages, mentions)
//   Phase 5 — presence + typing (broadcast only; zero DB writes)
//   Phase 6 — attachments upload flow + entity "Discuss" anchoring

export * from "./types";
